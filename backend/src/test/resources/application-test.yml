spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/checkmate_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.checkmate.chess: DEBUG

